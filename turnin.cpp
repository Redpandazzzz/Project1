#include <iostream>
#include <string>

using namespace std;

int main(int argc, char** argv) {
    if (argc > 2) return 1;
    string strings;
    string* arr;
    while (getline(cin, strings)) {
        if (strings.empty()) { cout << endl; continue; }
    }

}
        
